declare module "nineleap.enchant.js" {
    import {
        Core as OrigCore,
        Scene as OrigScene,
        Surface,
    } from "enchant.js";

    class Core extends OrigCore {
        constructor(
            width: number,
            height: number,
            startImage: string,
            endImage: string,
        );
        end(
            score: number,
            result: string,
        ): void;
    }

    class SplashScene extends OrigScene {
        constructor();
        image: Surface;
    }
}